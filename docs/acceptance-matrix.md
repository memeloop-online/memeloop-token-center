# Product requirement acceptance matrix

Snapshot: 2026-08-18. The current working tree, not earlier status messages, is authoritative for this matrix.

Status meanings:

- **Implemented**: source and a directly relevant repository test/contract exist. Exact local and isolated-PostgreSQL verification completed for this snapshot is listed below.
- **Pending verification**: implementation exists in the current working tree, but end-to-end, deployed, migration, browser, load, or security evidence required by the requirement is incomplete.
- **Pending wiring**: lower-level pieces exist, but the user-visible workflow or HTTP contract is incomplete.
- **Missing**: no adequate implementation was found.

## Architecture, storage, and deployment

| ID | Requirement | Status | Authoritative evidence | Remaining acceptance evidence/work |
|---|---|---|---|---|
| ARC-01 | New independent Rust service on `master` | Implemented | `Cargo.toml`, `src/main.rs`; repository branch is `master`; `/version` exposes injected build identity | Keep release metadata and compatibility text synchronized with releases. |
| ARC-02 | PostgreSQL is the production default | Implemented | `Config::from_env`, default `MTC_DATABASE_URL`; `DatabaseBackend::PostgreSql` | Deployed connection/failover test belongs to operations acceptance. |
| ARC-03 | SQLite for tests | Implemented | `DatabaseBackend::Sqlite`, working-tree migrations through v42; the current full Cucumber run passed 64/64 scenarios and 360/360 steps, while fresh isolated PostgreSQL 16 and 17 databases applied v1→v42 | Keep both backends in release CI; the fresh-database gate does not replace the pending imported-database upgrade rehearsal. |
| ARC-04 | S3 archive in production, simple memory/filesystem test backends | Implemented | `ArchiveStore::from_config` and bounded-read unit tests | Real S3 outage/retry and retention verification remains. |
| ARC-05 | Optimized large-volume request/event/aggregate schema | Pending verification | PostgreSQL partitions and query indexes under `migrations/postgres`; aggregate queries under `src/db/` | Provide EXPLAIN/latency evidence at imported-data scale and retention/partition lifecycle evidence. |
| ARC-06 | Low-memory gateway compared with CPA | Pending verification | bounded bodies, streaming archive, small SQL pool, image semaphore, and an exact-revision 15-minute CI harness are present | Run the harness for the release SHA, retain its JSON/log artifact and image digest, then verify the 256 MiB Pod cgroup peak/events; source and an untracked workflow are not execution evidence. |
| ARC-07 | Gateway/control/worker separation | Implemented | `RuntimeRole`, `router_for_role`, Helm role deployments | Verify public ingress never routes `/internal/v1/*`. |
| ARC-08 | Helm/K8s deployment | Implemented | `charts/memeloop-token-center` | Chart smoke test and upgrade/rollback acceptance remain. |
| ARC-09 | Production readiness/liveness separation | Implemented | anonymous `/livez`; bounded/coalesced DB+archive `/readyz`; deprecated `/healthz`; Helm probes; metrics integration tests 3/3 | Verify real dependency outage behavior in a deployed K8s environment. |
| ARC-10 | Prometheus metrics and ServiceMonitor | Implemented | control-only `metrics:read` `/metrics`, bounded labels, ServiceMonitor, metrics integration 3/3 and unit 3/3; Helm/kubeconform checks passed | Verify Prometheus scrape, alert rules, and dashboards in the deployed cluster. |
| ARC-11 | Partition maintenance does not wedge on DEFAULT rows | PostgreSQL verified | per-partition savepoints classify only PostgreSQL `23514` overlap as blocked, preserve rows in DEFAULT, report/warn and continue other tables/days; the fresh restricted PostgreSQL DB regression gate passed 3/3 including this path | This is fail-soft, not automatic evacuation: operations must safely drain each blocked date before retry creates its range partition. |
| ARC-12 | Globally unique request/event identity with partition pruning | Functional PostgreSQL verified | schema v21 non-partitioned locator tables own global IDs and carry partition timestamps; write/detail/finish/archive-import paths use exact `(id, created_at)` leaf access; SQLite lifecycle tests and real-PostgreSQL duplicate-backfill fail-closed plus single-leaf pruning regressions passed | The 141k-row migration lock-time and imported-scale EXPLAIN/latency evidence remain. |
| ARC-13 | Budget/rate hot paths do not scan unbounded audit history | Functional PostgreSQL verified | schema v22 transactionally maintains lifetime/reserved and account cumulative state, UTC-day plus rolling-week boundary detail; reserve/settle/cancel/grant-reversal no longer scan audit ledger/reservations; SQLite functional gates and the real-PostgreSQL concurrent reservation/settlement replay test passed | Production-history benchmark remains. Pre-v22 writers are not compatible after migration, so rollout requires a write barrier or a future dual-write bridge. |
| ARC-14 | Generation statistics avoid full job-table aggregation | Functional PostgreSQL verified | schema v23 terminal facts plus daily aggregates; ordinary bounded statistics use full-day aggregates and exact boundary facts, while pending/duration/cost filters use a bounded indexed raw fallback; SQLite backfill/idempotence/SQL-shape gates and the real-PostgreSQL generation aggregate integration passed | Imported-scale EXPLAIN/latency evidence remains. |
| ARC-15 | Optional archive/asset download throttling stays at the Kubernetes edge | External platform responsibility | asset APIs enforce authentication, tenant isolation, exact `Range`/`Content-Length` semantics and bounded streaming; the application contains no separate download limiter | Operators may configure bandwidth, request-rate and connection/concurrency controls in Higress/Ingress when needed. Their absence is not a Token Center release gate. Credential quota, model permission and AI request policies remain application responsibilities. |

## Credentials, tenant isolation, and memeloop web

| ID | Requirement | Status | Authoritative evidence | Remaining acceptance evidence/work |
|---|---|---|---|---|
| CRED-01 | Stable UUID identity and retry-safe rotation | Implemented | Required idempotency header and encrypted 24h replay; Cucumber replay; isolated K3S dev PostgreSQL proved 8 concurrent same-key calls produce generation 2 and four distinct keys produce generations 3–6 | Production rollout still needs normal monitoring; the isolated test schema was cleaned up. |
| CRED-02 | Migrated CPA credential can remain unchanged | Implemented | legacy credential table/API and CPA compatibility Cucumber scenario | Verify the production migrated Linux Codex identity after every importer run. |
| CRED-03 | Create credential with alias, model permission, quota and limits | Implemented | `POST /internal/v1/keys`, `KeyPolicy`, JSON Schema | Browser and deployed API regression still required for releases. |
| CRED-04 | Empty allowed-model list is fail-closed | Implemented | `KeyPolicy::allows_model` requires `*` or exact match; the default black-box Cucumber suite proves an empty list cannot list or call a priced model | Retain the regression when model discovery or policy syntax changes. |
| CRED-05 | List/reconcile by tenant and principal | Functional PostgreSQL verified | `GET /internal/v1/keys` and `Database::list_managed_keys_page` use an exclusive `(created_at, key_id)` cursor; `credential_billing_pagination` passed against SQLite and isolated PostgreSQL 16 with stable multi-page reads plus global, tenant-bound, principal-filtered and cross-tenant-denial cases | Re-run the same HTTP acceptance against each deployed release. |
| CRED-06 | Suspend/reactivate/revoke client credential | Implemented | `PATCH .../keys/{key_id}/status`; the default black-box Cucumber suite proves suspend/reactivate and terminal revocation | Re-run against deployed PostgreSQL before release. |
| CRED-07 | Service credential creation and retry-safe rotation | Implemented | service principal generations and required idempotency header; Cucumber exact-response replay; isolated K3S dev PostgreSQL proved 8 concurrent same-key calls produce generation 2 | Production rollout still needs normal monitoring; the isolated test schema was cleaned up. |
| CRED-08 | List/suspend/revoke service credentials | Implemented | list/status endpoints and DB methods; the default black-box Cucumber suite proves global-only status management and terminal revocation | Re-run against deployed PostgreSQL before release. |
| CRED-09 | Tenant-scoped service credential cannot cross tenant | Implemented | `require_service_tenant`, `management_tenant`; default Cucumber authorization-matrix scenario passed global, scoped, and downstream boundaries | Extend the matrix when new control endpoints are added. |
| CRED-10 | Least-privilege read/write scopes | Implemented | the default black-box Cucumber matrix independently issues all 23 managed-service scopes, exercises each matching operation family (including entitlements, import, quarantine, metrics, OAuth and plugins), and proves a cross-family operation remains 403 | Keep the supported-scope list, OpenAPI scopes and matrix in lockstep whenever an operation is added. |
| CRED-11 | Registration create is retry-safe | Implemented | tenant-qualified idempotency key, canonical request hash, encrypted 24h response | Document caller persistence/recovery; covered in `api-contract.md`. |
| CRED-12 | Lost registration response can be reconciled | Functional PostgreSQL verified | `credential_billing_pagination` drops a committed create response, proves a principal-only list remains ambiguous when two identities exist, replays the intended tenant-qualified idempotency key to select exactly one stable key/account/fingerprint, rejects changed-payload reuse, and verifies the list contains no secret | After the 24-hour encrypted response expires, secret recovery intentionally requires rotation; add a deployed delayed-recovery smoke test. |
| CRED-13 | User may use own credential only for own statistics/history | Implemented | `/self/v1/*` binds `key_id`; Rust authorization Cucumber and TypeScript Cucumber.js portal dogfood (Playwright browser driver only) passed with non-empty own requests/statistics | Continue IDOR tests for generation and conversation detail. |
| CRED-14 | User credential has no administrative authority | Implemented | separate service/client authentication and role routers; Cucumber denial step | None for core boundary. |
| CRED-15 | Credential groups remain presentation-only | Functional PostgreSQL verified | `security_routing_groups` proves direct route and route-group grants are the only routing subjects; adding, moving between, and removing credential-group memberships cannot change `/v1/models`, grants, provider candidates, or synchronous/persisted generation routing. A disabled retired CPA bridge retained as the legacy first account cannot hide the active group candidate or rewrite recorded route/history IDs. Routing APIs and database foreign keys reject credential-group grant use, and all three group kinds fail closed across tenants. The same matrix passed after migrations 1–48 on isolated PostgreSQL 17 | Keep credential-group IDs out of future routing-grant schemas and migrations. |

## Subscription credit and billing

| ID | Requirement | Status | Authoritative evidence | Remaining acceptance evidence/work |
|---|---|---|---|---|
| BILL-01 | Initial balance at credential provisioning | Implemented | atomic account/key/initial ledger creation in `Database::create_key` | None for basic flow. |
| BILL-02 | Idempotent subscription credit grant | Implemented | `POST .../grants`, account-qualified idempotency index; the default black-box Cucumber suite proves exact replay and changed-payload conflict | Add PostgreSQL concurrency coverage. |
| BILL-03 | Inspect balance and recent ledger from memeloop web | Functional PostgreSQL verified | managed-key responses expose available/reserved balances; `GET .../accounts/{account_id}/ledger` and `Database::list_account_ledger_page` use an exclusive `(created_at, entry_id)` cursor; SQLite and isolated PostgreSQL 16 HTTP E2E passed stable pagination, account binding, tenant denial, global access and missing-account behavior | Exercise the external MemeLoop Web consumer against a deployed release. |
| BILL-04 | Cancel/replace subscription after partial consumption | Implemented | versioned entitlement cancel/replace withdraw only FIFO-derived remaining credit and retain consumed audit; SQLite lifecycle/usage tests passed | PostgreSQL concurrency/upgrade execution and memeloop web integration remain. |
| BILL-05 | Exact entitlement reconciliation | Implemented | GET/PUT/cancel/replace API, stable subscription/cycle identities, tenant-qualified idempotency with 409 mismatch, `entitlement_adjustment` ledger; SQLite entitlement suite 5/5 and isolated PostgreSQL 17 `entitlements_postgres` 1/1 passed | Production subscription integration and rollout monitoring remain. |
| BILL-06 | Reserve before upstream and settle actual text usage | Implemented | `reserve_usage`/`settle_usage`; balance Cucumber scenario | Add crash/retry/stream disconnect fault tests at PostgreSQL scale. |
| BILL-07 | RPM, TPM, concurrency, daily/weekly/lifetime budgets | Implemented | `KeyPolicy`, atomic rate/runtime state and budget queries; default Cucumber scenarios explicitly cover RPM plus independent TPM, concurrency, daily, weekly and lifetime rejection | Add production-shape PostgreSQL concurrency/load evidence. |
| BILL-08 | Model price one-click sync from multiple fixed sources | Implemented | `src/pricing.rs`, `/model-prices/sync`, operator UI | Add deterministic fixture contract tests for conflicts/source failures and deployed egress verification. |
| BILL-09 | Manual price override preserved | Implemented | sync logic and manual `source` | Add API-level regression test. |
| BILL-10 | Generation pricing/permission/reservation/settlement | Implemented | price snapshot through schema v16; Seedance, ComfyUI and image Cucumber scenarios; `generation_jobs` tests passed 5/5 | Real providers and production-scale assets remain deployment verification. |
| BILL-11 | Async generation submission retry and cancellation | Implemented | two-phase `preparing` admission makes idempotency ownership, quota/rate/balance reservation, job creation and started event atomic before the single owner writes CAS; exact attach recovery covers lost commit acknowledgements. Queued cancellation is atomic and idempotent; schema v40 adds worker claims for fenced `cancelling` jobs, and mock-backed Cucumber proves ComfyUI upstream cancellation plus exactly-once refund. | Verify provider cancellation contracts against non-paid staging accounts before enabling real routes; incompatible pre-v22 writers still require a Recreate/scale-to-zero write barrier. |
| BILL-12 | Tiered token pricing including cached input/cache write | Implemented | schema v18 tier storage and immutable snapshot; OpenAI/Anthropic cache normalization; four-dimensional/tiered HTTP response; pricing unit suite and Cucumber cache/tier scenario passed | Verify real provider usage variants and catalog drift during dogfood. |

## Unified upstreams, OAuth, routing, and plugins

| ID | Requirement | Status | Authoritative evidence | Remaining acceptance evidence/work |
|---|---|---|---|---|
| UP-01 | API credentials and OAuth are first-class methods of one upstream resource | Implemented | one `upstream_accounts` table and `UpstreamCredential` enum; unified list/API | Keep UI and docs terminology aligned. |
| UP-02 | Stable upstream ID across API key/OAuth replacement and refresh | Implemented | atomic auth-kind/generation replacement keeps routes and credential history; `upstream_management` covers API→OAuth→API plus invalid-switch rollback, and Cursor Cucumber covers refresh | Run the included PostgreSQL concurrency case in the release database gate. |
| UP-03 | Unified upstream management UI, no separate OAuth tab | Implemented | one provider onboarding surface in `Operator.tsx`; TypeScript Cucumber.js browser dogfood verifies direct credential and OAuth/subscription methods in the same tab | Re-run browser acceptance against each deployed release. |
| UP-04 | Cursor direct PKCE and refresh | Implemented | `src/oauth.rs`; encrypted account+generation refresh lease, durable pending finalize, bounded DB-only retry, proactive worker and Cursor Cucumber scenario | Real provider smoke test still required. |
| UP-05 | Native OpenAI Codex OAuth | Implemented | fixed-endpoint device authorization, PKCE, signed-token verification, durable cross-replica polling/finalization, stable-account reauthorization and mocked protocol coverage | Live-provider acceptance remains an explicit deployment gate. |
| UP-06 | Retired legacy connection continuity | Read-only compatibility | historical account, route and request references remain readable; new creation, authorization, refresh, rotation and inference are blocked | Remove the encrypted legacy decoder only after the retention window and an audited data export. |
| UP-07 | Provider route creation and priority | Implemented | idempotent create, priority validation, provider-protocol compatibility and `resolve_upstream`; route-management test passed | Multi-route failover policy remains a separate product decision. |
| UP-08 | Route listing | Implemented | tenant/global `GET /model-routes`; route-management tenant/scope tests and operator browser flow passed | Re-run against deployed PostgreSQL data. |
| UP-09 | Route update/disable/delete | Implemented | optimistic PUT/PATCH, disabled-and-unreferenced DELETE, history protection; `route_management` 1/1 and the TypeScript Cucumber.js operator flow passed | PostgreSQL-specific route concurrency test remains. |
| UP-10 | Plugin contributes provider config and OAuth adapter | Implemented | versioned `oauth-adapter-v1`/`cursor_pkce` contribution, load-time credential-contract validation, raw provider-schema boundary and installable example; `plugin_provider_gateway` creates the provider and route through the real management API | Real third-party install/upgrade remains deployment evidence; new flow kinds require a new explicit ABI value. |
| UP-11 | Plugin contributes traffic/rewrite policy safely | Implemented | the real Wasm fixture covers deny/rewrite across text, Images, Seedance and ComfyUI submission; model permission, route and price are rechecked after rewrite; `plugin_product` proves fuel, epoch, 32 MiB, trap, streaming and oversize fail-closed boundaries | Real third-party package soak remains deployment evidence. |
| UP-12 | Plugin configuration is declarative, scoped and reaches the traffic ABI | Implemented | object-root non-secret JSON Schema validation, RJSF operator form, global/tenant precedence, optimistic versions, idempotent writes, bounded cache invalidation and a Wasm integration fixture that traps unless the stored value reaches `request-context.config-json` | Run a multi-replica deployment test to retain evidence that an update becomes visible on every replica within the documented five-second cache window. |
| UP-15 | Third-party plugin packaging/distribution | Pull/verify/install MVP implemented | standard ORAS/Cosign publishing contract; digest-only references, exact source allowlist, mandatory public-key verification, descriptor and streamed-write bounds, traversal rejection, shared runtime package validation, Linux atomic no-replace install and provenance receipt; local mock-registry tests pass the install, signature rejection, traversal, invalid-package cleanup and no-overwrite paths | Add first-party publish automation and Helm init-container values, then retain evidence from a signed private-registry artifact and immutable-volume rollout/rollback. Runtime in-place upgrades remain deliberately unsupported. |
| UP-13 | Unified upstream lifecycle management | Implemented | server-derived `can_refresh`/`can_rotate`/`can_reauthorize`, optimistic PUT/PATCH, bounded health, audit-safe DELETE, stable cross-method replacement and generation-CAS refresh; SQLite fault injection proves encrypted pending recovery without a second remote refresh | Run the included PostgreSQL lease race and real OAuth/provider probes before release. |
| UP-14 | Executable third-party provider adapts a non-OpenAI API without seeing credentials | Implemented | WIT 0.2 `buffered-v1` prepare/normalize, explicit manifest capability/version, same-origin/SSRF/timeout/4 MiB core transport and credential injection; `plugin_provider_gateway` verifies real Wasm, non-OpenAI mock shape, stable account rotation, core pricing and request/response archive | Streaming adapters deliberately fail closed; a future ABI requires a new explicit version and bounded streaming design. |

## Client protocols and multimodal generation

| ID | Requirement | Status | Authoritative evidence | Remaining acceptance evidence/work |
|---|---|---|---|---|
| PROTO-01 | Codex via OpenAI Responses | Implemented | `/v1/responses`, OpenAI proxy pipeline | Real Codex dogfood regression remains release evidence. |
| PROTO-02 | Claude Code via Anthropic Messages/count_tokens | Implemented | `/v1/messages*`; Claude Cucumber scenario | Real Claude Code smoke test remains. |
| PROTO-03 | Copilot/Cursor/WorkBuddy/OpenAI-compatible clients | Pending verification | `/v1/chat/completions`, embeddings and native provider routes | WorkBuddy and Cursor/Copilot real-client matrices are not present. |
| PROTO-04 | OpenAI-compatible model discovery | Implemented | `/v1/models` filters priced models through policy; default Cucumber proves an empty policy is deny-all | Add an explicit wildcard-policy regression. |
| MM-01 | Seedance video generation | Implemented | Seedance driver, worker polling/archival and Cucumber scenario | Real Volcengine OAuth/API credentials and failure-code smoke test remain. |
| MM-02 | ComfyUI image/video generation | Implemented | ComfyUI driver, administrator-owned workflow validation and Cucumber scenario | Real local and cloud ComfyUI workflows need deployed verification. |
| MM-03 | OpenAI Images forwarding and billing | Implemented | `/v1/images/generations`, image Cucumber scenario | Real provider smoke test remains. |
| MM-04 | Codex Responses image tool exposed through Images API | Implemented | responses-tool transformation and Cucumber scenario | Verify through the deployed relay rather than direct upstream. |
| MM-05 | Large generated asset archival without gateway memory growth | Pending verification | worker streams bounded assets; synchronous image body/semaphore bounds | Add 100–500 MiB asset load/soak test with memory measurements. |

## Requests, statistics, conversations, and migration

| ID | Requirement | Status | Authoritative evidence | Remaining acceptance evidence/work |
|---|---|---|---|---|
| OBS-01 | Started/finished realtime request monitoring | Implemented | PostgreSQL request events and resumable SSE API; Cucumber stream assertion | Add reconnect/cursor/retention/load verification. |
| OBS-02 | Efficient operator aggregate statistics | Functional PostgreSQL verified | default 30-day/maximum 93-day bounded statistics with time, credential, model, protocol, status, error, upstream, route, duration, cost, alias and principal filters; isolated PostgreSQL/SQLite observability suite passed 2/2 | Imported-production-size EXPLAIN/latency benchmark remains. |
| OBS-03 | Operator request/error archive detail | Implemented | bounded archive lookup with tenant/global enforcement | Add redaction policy and configurable retention acceptance. |
| OBS-04 | Self-service CPAMP-style statistics | Implemented | self stats/requests/generations/conversations UI and APIs; TypeScript Cucumber.js browser dogfood verifies non-empty own-only statistics, filters, detail, pagination, limits and bilingual currency formatting | Deployed browser acceptance and product-level CPAMP comparison remain. |
| OBS-05 | Browse old/high-volume request history | Functional PostgreSQL verified | descending `(before_created_at, before_id)` cursor plus the complete operator filter set and self-service stable-key boundary; isolated PostgreSQL/SQLite observability suite passed 2/2 | Imported-production-size benchmark remains; page limits are 100/500. |
| CONV-01 | Explicit client session/turn/parent/branch/compaction metadata | Implemented | `ConversationHints`, observation/edge persistence and passing Cucumber compaction scenario | Expand real Codex/Claude metadata fixture corpus. |
| CONV-02 | Infer logical conversations without sufficient metadata | Implemented and bounded | tenant/principal-scoped semantic atoms and Merkle prefix nodes; empty histories cannot supply prefix evidence; low-confidence same-client matches persist as visible candidate edges without merging clusters | Validate precision/recall on real imported sessions and tune the 30-minute candidate window from measured data. |
| CONV-03 | Compression remains related to prior conversation | Implemented | `compacts` relation and explicit parent evidence; default Cucumber covers consecutive compactions followed by a branch | Expand the real-client fixture corpus. |
| CONV-04 | OpenAI Responses `previous_response_id` preserves parent relation | Implemented | schema v16 response-ID storage plus streaming/non-streaming black-box scenarios; expanded protocol feature passed 6 scenarios/36 steps | Keep real-client fixture corpus current. |
| CONV-05 | High-volume conversation list and detail remain bounded | Functional PostgreSQL verified | v25 key/cluster projection, list and detail keyset cursors, direct ownership point lookup, per-page edge cap; `conversation_pagination` passed 6/6 including the isolated PostgreSQL 17 110k/key-scope case, in addition to the SQLite EXPLAIN coverage | Run `tests/load/conversation_explain.py` against the imported PostgreSQL dogfood snapshot and retain its final EXPLAIN/latency report. |
| CONV-06 | Archive conversation projection is replay-safe and atomic with import metadata | SQLite fault-injection verified | one transaction covers observation/edge/projection, refs, import record and checkpoint; injected reference failure proves zero state change and replay proves stable counts | Repeat the fault/replay gate on PostgreSQL before live archive apply. |
| MIG-01 | Initial CPA/CPAMP usage/alias/price import | Fixture PostgreSQL verified | isolated PostgreSQL 17 acceptance ran source/target counts, distinct IDs, token/cost totals, one error and request/error body-gap samples; request facts, request daily, usage hourly and usage daily tables each counted the four final events exactly once with USD, normalized protocols, error and duration buckets, including after exact replay | This was a synthetic CPAMP SQLite fixture, not a live CPA/CPAMP snapshot; real immutable-snapshot reconciliation remains. |
| MIG-02 | Repeatable incremental import until cutover | Fixture PostgreSQL verified | the same isolated acceptance replayed the initial import, imported a delayed overlap row and a new watermark row, proved deterministic IDs, tenant/source checkpoint isolation and fail-closed unmapped behavior | Run repeated live increments behind a final CPA write barrier and prove zero omissions before traffic shift. |
| MIG-03 | Preserve original CPA client credential | Implemented | legacy credential registration/import path | Validate exact production Linux Codex credential without exposing it in logs. |
| MIG-04 | Rehydrate cpa-session-archive bodies and session observations | Functional PostgreSQL verified | real v0.7.21 schema-v1 plus identity-aware schema-v2 importer coverage includes mixed-version fail-closed preflight, exact linkage, sealed-input CAS apply/replay, checkpoints, observations and no-overwrite; isolated PostgreSQL 17 `session_archive_import` and `session_archive_unlinked` each passed 3/3 | No S3/MinIO or live session-archive import has been run; execute the isolated bucket acceptance and production reconciliation before live apply. |

## UI, schemas, API governance, and security

| ID | Requirement | Status | Authoritative evidence | Remaining acceptance evidence/work |
|---|---|---|---|---|
| UI-01 | Chinese/English i18n switch | Implemented | `web/src/i18n.tsx`; TypeScript Cucumber.js browser dogfood switches Chinese operator and portal UI to English and asserts translated tabs/headings | Extend the browser crawl as new screens are added. |
| UI-02 | Light and dark themes | Implemented | theme CSS/control; TypeScript Cucumber.js browser dogfood switches dark/light themes and exercises the portal at mobile width | Add an approved sanitized visual-regression layer only if pixel-level stability becomes required; the credential-bearing suite records no screenshots. |
| UI-03 | Website icon at appropriate sizes | Implemented | icons under `web/public`; TypeScript Cucumber.js browser dogfood fetches the linked PNG and checks its media type/content | Recheck deployed favicon cache behavior after asset revisions. |
| UI-04 | JSON Schema-rendered simplified configuration | Implemented | `/schemas`, RJSF operator forms/templates, CSP-safe `safeValidator`, and shared Rust/browser parity fixtures in `tests/fixtures/schema-parity.json` | Re-run the browser CSP/header check after every deployed asset or ingress revision. |
| SCHEMA-01 | Provider/plugin schemas enforced server-side | Implemented | mature `jsonschema` Draft 2020-12 validator is authoritative for raw plugin manifests and every provider config/credential write; remote references and unsupported renderer keywords fail closed; Rust tests, shared browser contract tests and black-box Cucumber cover invalid/unknown values | Extend the shared parity fixture when the supported declarative schema profile grows. |
| API-01 | Discoverable OpenAPI contract | Implemented | `openapi/openapi.yaml`; Redocly validation passes and source/OAS route-set comparison covers every registered versioned API route | Add CI lint and schema-level conformance; the service does not serve `/openapi.json`. |
| API-02 | Explicit idempotency, tenant and error contract | Pending verification | OpenAPI and `docs/api-contract.md` | Add black-box contract tests for every documented status/response. |
| API-03 | API version/deprecation metadata | Implemented | protected control `/version`, `X-MTC-API-Version: v1`, `/healthz` deprecation headers, build injection; metrics integration tests 3/3 | Define a concrete minimum deprecation duration/sunset policy before the first removal. |
| SEC-01 | Authenticate before buffering protected bodies | Implemented | control/gateway authentication middleware plus a default black-box Cucumber scenario proving malformed and oversized unauthenticated bodies return unauthorized before parsing/size enforcement; once authenticated, malformed/missing/unknown JSON and invalid query/path extractors return the same generic JSON 400 envelope | Deployed reverse-proxy verification remains. |
| SEC-02 | Tenant isolation/IDOR resistance | Implemented | tenant/key constraints plus passing Rust Cucumber global/scoped/downstream authorization matrix and TypeScript Cucumber.js cross-tenant 403 assertion | Extend to new endpoints and fuzz IDs/filters. |
| SEC-03 | SQL/command/path injection protection | Functional PostgreSQL verified | `security_injection_matrix` sends quote/comment/boolean/UNION/stacked payloads through tenant, principal, alias, model, error, source and cursor inputs, verifies exact results and schema survival; SQLite always runs, and a fresh isolated PostgreSQL 17 database applied migrations 1–48 before the matrix passed | Keep `MTC_REQUIRE_POSTGRES_SECURITY=1` for any release gate outside CI; add property/fuzz generation without replacing the deterministic matrix. |
| SEC-04 | Secret-safe HTTP errors/logging | Implemented for core failure families | task-local tracing capture plus distinct canaries cover proxy, OAuth, CPA import, database, object-store conversion and an actual archive-reaper worker failure; existing plugin tests cover guest-controlled log content | Extend canary coverage whenever a new external error source or tracing field is added. |
| SEC-05 | Public browser security headers | Implemented | CSP, HSTS, no-store, nosniff, frame and permissions policy middleware | Recheck at ingress/CDN after every deployment. |

## Verification evidence for this snapshot

The OpenAPI document was parsed and linted with:

```bash
npx --yes @redocly/cli@2.20.0 lint openapi/openapi.yaml
```

The command exited successfully and declared the API description valid. Its only four warnings are the recommended-rule warnings that the non-erroring `/healthz`, `/livez`, `/readyz`, and fallback-capable `/portal` operations have no invented 4xx response.

The following results were supplied by the implementation/test agents for changes present in this working tree. They were completed at the points reported; this documentation task reran the current default Cucumber suite and browser dry-run, but did not rerun the entire combined suite after every concurrent edit:

- On the current v42 working tree, the full Rust Cucumber run passed 5 features, 64/64 scenarios and 360/360 steps; 63 scenarios use SQLite/memory storage and the CPAMP scenario uses isolated PostgreSQL. This is not live-deployment evidence.
- The current TypeScript Cucumber.js browser suite resolved and passed 11/11 scenarios and 87/87 steps. Playwright is only the browser driver inside those steps, and the suite records no screenshots, traces or video.
- On the stable v23 tree, `cargo test --all-targets` passed; the library suite passed 73 tests, the targeted DB suite 20/20, generation jobs 5/5, and the default Cucumber run 39 scenarios/218 steps. The Cucumber result includes the expanded six-scenario/36-step conversation-protocol feature.
- Earlier `npm run typecheck`, `npm run build`, and Playwright-runner 2/2 evidence passed, including operator and client-credential portal dogfooding; the current browser runner is the TypeScript Cucumber.js suite recorded above.
- `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all -- --check`, and the diff check passed at the reported combined-tree gate.
- Credential rotation was exercised against an isolated schema on the K3S development PostgreSQL instance and cleaned up afterward: eight concurrent client-key rotations with one idempotency key all replayed generation 2; four distinct keys serialized as generations 3–6; eight concurrent service-credential rotations replayed generation 2.
- `entitlements_postgres` ran against an isolated temporary PostgreSQL database and restricted role: 1/1 passed, then cleanup verified zero remaining test database/role objects.
- `route_management` passed 1/1 and `upstream_management` passed 2/2; their earlier targeted operator Playwright-runner management flows passed, and the current equivalent is covered by TypeScript Cucumber.js.
- The cache/service-tier pricing implementation passed the 62-test library gate plus its seven-step Cucumber scenario. HTTP types expose all four price dimensions and tier metadata.
- The earlier v1–v20 PostgreSQL gate used a fresh temporary database and restricted role. `postgres` passed 2/2, `entitlements_postgres` passed 1/1 and `observability_filters` passed 2/2 (one PostgreSQL and one SQLite test). `schema_migrations` reported exactly versions 1–20 with no gaps, and the request/request-event partition count was 20. Cleanup verified zero test databases/roles; the three remote `kubectl port-forward` processes created by this gate were identified by PID plus full argv and removed; its local tunnel/temp files were removed. A pre-existing, unrelated port-forward on 15432 was deliberately retained.
- `session_archive_import` passed its SQLite/CAS integration 1/1 and module unit tests 2/2. No PostgreSQL/S3 or live session archive import was performed.
- The isolated K3S PostgreSQL CPAMP acceptance passed initial import, exact replay, delayed-overlap increment, new-watermark increment, deterministic IDs, tenant/source checkpoint isolation, and fail-closed unmapped behavior. Its final synthetic fixture reconciled 4 source rows to 4 distinct target requests, 70 input tokens, 26 output tokens and 244 cost micros. This was not live CPAMP data.
- `credential_billing_pagination` passed its complete HTTP flow against both a fresh SQLite database and an isolated local PostgreSQL 16 database. It exercised credential and ledger keyset pagination, inserts between pages, global/tenant/principal scopes, cross-tenant denial, missing accounts, lost create-response reconciliation, intentionally ambiguous principal matches, changed-payload idempotency rejection, no-secret list responses, and stable identity/account/policy/balance across rotation. The exact temporary PostgreSQL container was deleted after the gate.
- Strict Helm lint passed default and dev values. Render/schema/kubeconform gates passed the default 23-resource and canary 9-resource sets, including negative schema checks for empty enabled network rules. The fail-closed selector values were not applied to the live dogfood release.
- Fresh isolated PostgreSQL 16 and 17 databases applied migrations v1→v42. The final PostgreSQL 16/MinIO run passed `tests/postgres.rs` 5/5 plus archive staging 9/9, generation jobs 19/19, managed OAuth imports 11/11, proxy atomicity 2/2, conversation pagination 7/7, Cloud and regular entitlements, observability filters, archive quarantine 6/6, session-archive exact/unlinked paths, usage analysis 4/4 and legacy credential import. This proves the fresh sequence and focused behaviors; it does not prove a production-snapshot lock-time or live rollout.
- The current v42 working tree completed deterministic 100k-query EXPLAIN gates and bounded concurrency/RSS checks. A retained current-image soak and live API3 rollout remain release gates.

These results do **not** replace live external OAuth/provider, production CPAMP/session-archive reconciliation, imported-data load/soak, or public-ingress/NetworkPolicy verification. In particular, passing the CPAMP PostgreSQL fixture does not imply that cpa-session-archive bodies were imported into PostgreSQL, and the live dogfood NetworkPolicy was still permissive at audit time.
