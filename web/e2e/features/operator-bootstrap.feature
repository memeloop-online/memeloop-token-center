# language: zh-CN
功能: 操作台安全解析租户范围
  操作台必须先解析服务凭据可见的租户，再加载任何带租户范围的资源。

  背景:
    假如 dogfood 服务已有隔离租户、统一上游、请求记录和多模态价格

  场景: 首次连接与凭据替换不会短暂查询或展示错误租户范围
    当 操作台依次验证单租户、多租户、租户发现失败和快速凭据替换
    那么 只有当前凭据解析出的安全租户范围会提交到资源 API
